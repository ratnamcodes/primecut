"""Render spec (Phase 2).

A declarative description of what to render: which moments, in what order,
at what format, with which caption style and crop. Produced by the composer
and consumed by the renderer, so composition decisions are inspectable data
before any ffmpeg process starts. Filled in by Phase 2.
"""
