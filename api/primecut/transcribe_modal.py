"""WhisperX on Modal (Task 3).

Defines the Modal app and GPU function that runs WhisperX (transcription,
forced alignment, and speaker diarization via the Hugging Face token) on the
mezzanine audio and returns word-level timestamps. This is the only module
that talks to Modal directly. Filled in by Task 3.
"""
